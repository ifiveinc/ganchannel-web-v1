# Web版 がんちゃんねる 開発規約

> この開発規約をまとめたファイルをAIエージェントに共有して、規約通りに開発してもらうことを目的として作成。

---

## 1. 本規約について

### 1.1 目的

本規約は、複数人でのアプリケーション開発において、以下を実現することを目的とする。

- 開発方法やコードの書き方を統一する
- メンバー間の認識違いを減らす
- Git上での競合や手戻りを防ぐ
- コードの品質と保守性を維持する
- 誰が何を担当しているかを明確にする
- 安全に本番環境へ変更を反映する
- 新しいメンバーが参加しやすい状態を作る

本規約は、メンバーを制限するためのものではなく、チーム内のコミュニケーションを円滑にし、開発を安全に進めるための共通ルールとして使用する。

### 1.2 対象範囲

本規約は、以下の作業を対象とする。

- 要件・仕様の管理
- タスク管理
- Git・GitHubの運用
- コーディング
- コードレビュー
- テスト・動作確認
- ライブラリ・環境変数の管理
- デプロイ
- ドキュメント管理
- AI開発支援ツールの利用

画面の色、余白、フォント、コンポーネントの見た目などについては、別途定めるデザイン規約に従う。

### 1.3 規約の優先度

各規約は、以下の2種類に分類する。

- **必須**：原則として必ず守る
- **推奨**：可能な限り守る

規約を守ることが難しい事情がある場合は、独断で省略せず、チーム内で相談する。

---

## 2. プロジェクトの基本情報

| 項目 | 内容 |
| --- | --- |
| プロジェクト名 | Web版 がんちゃんねる |
| 開発するアプリ | Webアプリ |
| 開発目的 | 岩手大学の情報と統合するアプリ「がんちゃんねる」を作るため |
| 想定利用者 | 岩手大学関係者（学生・職員）・学外の人間（他大生・受験生） |
| リポジトリURL | https://github.com/ifiveinc/ganchannel-web-v1 |
| タスク管理ツール | GitHub Issues / GitHub Projects / Notion |
| 連絡手段 | Slack / LINE |
| フロントエンド | TypeScript / React 19 / React Router v7（フレームワークモード・SSR有効） |
| バックエンド | React Router v7 の `loader`・`action`（専用のAPIサーバーは設けない） |
| ビルド・実行 | Vite 6 / `@react-router/serve` |
| スタイル | Tailwind CSS v4 |
| データベース | Supabase（PostgreSQL） |
| インフラ・デプロイ先 | Render（常駐Nodeプロセス、`react-router-serve`） |
| デザイン資料 | 作成中 |
| 仕様書 | 作成中 |

上記はMVP版（2026/08/06）時点の構成である。
MVP公開後、カンパニー全体の方針に従い**Next.jsへ移行する**予定がある。
経緯と移行時の扱いは [decisions/0002-framework-react-router.md](../decisions/0002-framework-react-router.md) を参照。

---

## 3. 役割と意思決定

チームの体制（役割ごとの担当者）は本規約では管理しない。以下は、意見が分かれた場合の最終決定権を役割の種類で示したもので、担当者の割り当てとは独立して成り立つ。

### 3.2 意思決定者

意見が分かれた場合の最終決定者を、あらかじめ決めておく。

| 決定事項 | 最終決定者 |
| --- | --- |
| プロジェクト全体の優先順位 | PM |
| 機能の追加・削除 | PM |
| 画面・デザイン | DE |
| 技術構成・ライブラリ | SWE |
| API・データベース設計 | SWEまたは各担当者 |
| 細かな実装方法 | 実装担当者 |
| 本番リリース | PM・IE |

重要な判断を行った場合は、決定内容と理由を文書として残す。

---

## 4. 情報管理

### 4.1 ツールの役割

情報の重複や分散を防ぐため、各ツールの役割を明確にする。

| 情報 | 管理場所 |
| --- | --- |
| 正式な開発タスク | GitHub Issues |
| タスクの進捗状況 | GitHub Projects |
| 要件・仕様書 | Notionまたは指定ドキュメント |
| デザイン | Figmaまたは指定ツール |
| コード | GitHub |
| 日常的な連絡・相談 | DiscordまたはSlack |
| 重要な意思決定 | Notion、Issue、議事録 |
| 不具合報告 | GitHub Issues |

チャット上の会話だけで、重要な仕様や決定事項を完結させない。

### 4.2 進捗共有

各メンバーは、作業状況がほかのメンバーから確認できる状態にする。

最低限、以下を更新する。

- Issueの担当者
- Issueのステータス
- 作業中のブランチ
- Pull Requestの状態
- 作業上の問題や不明点

担当者はGitHub IssueのAssignees、ステータスはGitHub Projectsのボードで管理する（§5.1・§5.5）。

長期間作業が止まる場合や、予定より遅れる場合は、早めにチームへ共有する。

Issue完了時は、GitHub上の更新だけでなくSlackでも一言共有し、タスク管理スプレッドシートの完了プルダウンも選択する。

---

## 5. タスク・Issue管理規約

### 5.1 基本ルール

**必須**

- 原則として、コードを変更する前にIssueを作成する
- Issueごとに担当者を設定する
- Issueには完了条件を記載する
- 仕様が不明確な場合は、実装前に確認する
- 発見した不具合は口頭だけで済ませず、Issueとして残す
- 作業完了後はIssueを更新または終了する

軽微な誤字修正など、Issueを作成する必要がないとチームで合意した作業は例外とする。

Issue作成時点では、担当者欄には役職（PM/DE/SWE/IE/WebD）のみを記載してよい。役職内の誰が担当するかは着手時にチームで調整し、決まり次第GitHub Issueの「Assignees」に個人を設定する。Assigneesに設定できるのは、リポジトリへのアクセス権を持つメンバー（Collaborator）のみ。Assigneesはいつでも変更でき、担当が変わった場合は都度更新する。

### 5.2 Issueの記載項目

Issueには、原則として以下を記載する。

```md
## 概要

何を実装・修正するのかを記載する。

## 目的

なぜこの作業が必要なのかを記載する。

## 実装内容

- 実装する内容1
- 実装する内容2
- 実装する内容3

## 完了条件

- [ ] 条件1
- [ ] 条件2
- [ ] 条件3

## 担当・優先度・期日

- 担当：
- 優先度：
- 期日：

## 関連資料

- デザイン：
- 仕様書：
- 関連Issue：

## 補足

実装時の注意点や、まだ決まっていない事項を記載する。
```

担当欄には役職（PM/DE/SWE/IE/WebD）を記載する。役職内の誰が担当するかは着手時にチームで調整し、決まり次第GitHub Issueの「Assignees」に個人を設定する。

### 5.3 Issueタイトル

Issueタイトルは、内容が具体的に分かる名称にする。

良い例：

```text
ユーザー登録フォームを実装する
スマートフォン表示でヘッダーが崩れる問題を修正する
団体詳細ページに活動実績欄を追加する
```

避ける例：

```text
登録機能
修正
画面を作る
バグ対応
```

### 5.4 タスクの大きさ

1つのIssueが大きすぎる場合は、複数のIssueに分割する。

目安として、以下のいずれかに該当する場合は分割を検討する。

- 複数の独立した機能を含む
- 担当者が複数人になる
- 完了までに長期間かかる
- フロントエンドとバックエンドで作業が明確に分かれる
- Pull Requestの変更量が大きくなる

分割した各Issueは、`develop`から直接ブランチを作成する（§6.1）。親Issueのブランチから子Issueのブランチを分岐させる、といった入れ子構成は行わない。ブランチのベースが枝ごとに変わりPRが把握しにくくなるうえ、親ブランチの更新のたびに子ブランチのrebaseが発生するため。

分割したIssue間に実装順の依存がある場合は、依存元のIssueを`develop`へマージしてから、依存先のIssue用ブランチを作成する。

### 5.5 GitHub Projectsの運用

進捗状況はGitHub Projects（Board）で管理する（§4.1）。

**列（ステータス）**

| ステータス | 意味 |
| --- | --- |
| Todo | 未着手 |
| In Progress | 実装中 |
| In Review | Pull Requestを作成し、レビュー待ち／対応中 |
| Done | マージ・クローズ済み |

新規Issueはリポジトリの自動追加設定（Workflows › Auto-add to project）により`Todo`へ自動的に追加される。着手時に`In Progress`へ、Pull Request作成時に`In Review`へ、それぞれ手動でステータスを更新する。`Done`への移動は、Issueに紐づくPull Requestがマージされてクローズされた際に自動化するか、手動で更新する。

**カスタムフィールド**

| フィールド | 型 | 値 |
| --- | --- | --- |
| 優先度 | Single select | 最優先／高／中／低 |
| 期日 | Date | 締切・目安日 |

担当者（役職ではなく個人）はカスタムフィールドを設けず、GitHub標準のAssignees機能を使う（§5.1）。優先度・期日はIssue作成時に本文へ記載した内容と一致させ、内容が変わった場合は両方を更新する。

---

## 6. Git・ブランチ運用規約

### 6.1 使用するブランチ

本プロジェクトでは、以下の3層構成を使用する。

| ブランチ | 役割 | 分岐元 | マージ先 |
| --- | --- | --- | --- |
| `main` | 本番環境へ反映するコードを管理する | — | — |
| `develop` | 各メンバーの作業内容を集約する。リポジトリの既定ブランチ | `main` | `main` |
| 作業ブランチ | 各機能や修正を実装する。メンバーが個別に作成する | `develop` | `develop` |

作業ブランチは`develop`から分岐し、`develop`へ戻す。`main`へは`develop`からのみマージする。

マージ後の作業ブランチは、原則として削除する。

上記以外のブランチが必要になった場合は、チーム内で相談して追加する。

### 6.2 mainブランチ・developブランチ

**必須**

- `main`・`develop`ブランチへの直接pushは禁止する
- 両ブランチへの変更はPull Requestを通して行う
- 動作確認やレビューが完了していないコードをマージしない

`main`ブランチ

- 常に本番環境へ反映できる状態を維持する
- `develop`以外からのマージは、緊急対応（§25）を除いて行わない
- リリース時はタグを付け、どの時点を公開したか追えるようにする

`develop`ブランチ

- 常にLint・型チェック・ビルドが通る状態を維持する
- 作業ブランチのマージ先は、原則としてこのブランチとする

### 6.3 ブランチ名

ブランチ名は、以下の形式とする。

```text
作業種別/Issue番号-作業内容
```

例：

```text
feature/12-user-signup
fix/24-mobile-header
docs/31-update-readme
refactor/42-user-service
test/53-add-login-test
chore/61-update-eslint
```

使用する作業種別は以下とする。

| 種別 | 用途 |
| --- | --- |
| `feature` | 新機能の追加 |
| `fix` | 不具合修正 |
| `docs` | ドキュメント修正 |
| `refactor` | 機能を変更しないコード整理 |
| `test` | テストの追加・修正 |
| `chore` | 設定、ライブラリ、環境整備 |
| `style` | CSSや表示上の修正 |

### 6.4 作業開始時の手順

作業開始前に、最新の`develop`ブランチを取得する。

```bash
git switch develop
git pull origin develop
```

作業ブランチを作成する。

```bash
git switch -c feature/12-user-signup
```

すでに作業ブランチが存在する場合は、新しく作成せず既存ブランチへ移動する。

### 6.5 競合を防ぐためのルール

- 作業開始時に担当するIssueを明確にする
- 同じファイルを複数人で大きく変更する場合は事前に相談する
- 長期間ブランチを放置しない
- 定期的に`develop`の変更を取り込む
- コンフリクトが発生した場合は、関係する担当者と確認しながら解消する
- 他人が書いた処理を、意図を確認せず削除しない

---

## 7. コミット規約

### 7.1 コミットの単位

**必須**

- 1つのコミットには、原則として1つの意味を持たせる
- 無関係な変更を同じコミットに含めない
- 動作しない状態を長期間コミットしない
- デバッグ用ファイルや不要な生成物をコミットしない

例えば、以下の変更は可能な限り別のコミットにする。

- 新機能の追加
- CSSの修正
- READMEの更新
- 不具合の修正
- ライブラリの更新

### 7.2 コミットメッセージ

コミットメッセージは、以下の形式とする。

```text
変更種別: 変更内容
```

例：

```text
feat: ユーザー登録フォームを追加
fix: モバイル表示でヘッダーが崩れる問題を修正
docs: READMEに環境構築手順を追加
refactor: API通信処理を共通関数に分離
test: ログイン処理のテストを追加
chore: ESLintの設定を更新
style: 団体詳細ページの余白を調整
```

### 7.3 使用する変更種別

| 種別 | 内容 |
| --- | --- |
| `feat` | 新機能 |
| `fix` | 不具合修正 |
| `docs` | ドキュメント |
| `style` | 見た目やフォーマット |
| `refactor` | リファクタリング |
| `test` | テスト |
| `chore` | 設定、ライブラリ、環境整備 |

### 7.4 避けるコミットメッセージ

以下のように、変更内容が分からないメッセージは使用しない。

```text
修正
更新
作業
いろいろ変更
完成
一旦コミット
```

---

## 8. Pull Request規約

### 8.1 基本ルール

**必須**

- `main`・`develop`への変更はPull Requestを通す
- Pull Requestには関連Issueを記載する
- 作成者以外のメンバーがレビューする
- 指摘への対応が完了してからマージする
- 画面変更がある場合はスクリーンショットを添付する
- Pull Request作成前に、最低限の動作確認を行う
- 1つのPull Requestに複数の無関係な機能を含めない

### 8.2 Pull Requestの大きさ

Pull Requestは、レビュー可能な大きさに抑える。

**推奨**

- 1つの機能または1つの修正に限定する
- 大きな変更は複数のPull Requestに分割する
- 変更ファイル数やコード量が多くなる場合は事前に共有する
- 数百行を大きく超える場合は分割を検討する

自動生成ファイルやlockファイルは、変更量の目安から除外してよい。

### 8.3 Pull Requestテンプレート

```md
## 概要

このPull Requestで行った変更を簡潔に記載する。

## 関連Issue

close #

## 変更内容

-
-
-

## 確認方法

1.
2.
3.

## 動作確認

- [ ] ローカル環境で起動できる
- [ ] 変更した機能が正常に動作する
- [ ] 既存機能が壊れていない
- [ ] Lintエラーがない
- [ ] ビルドが成功する
- [ ] 不要なログを削除した
- [ ] PC表示を確認した
- [ ] スマートフォン表示を確認した

## スクリーンショット

画面変更がある場合に添付する。

## レビューしてほしい点

特に確認してほしい部分や、不安な点を記載する。

## 補足

実装上の判断、未対応事項、今後の課題などを記載する。
```

### 8.4 Draft Pull Request

実装途中でも、以下の場合はDraft Pull Requestを作成してよい。

- 実装方針について相談したい
- 早い段階でレビューを受けたい
- 大きな変更の進捗を共有したい
- ほかのメンバーと連携する必要がある

Draft状態のPull Requestは、原則としてマージしない。

---

## 9. コードレビュー規約

### 9.1 レビューの目的

コードレビューは、実装者の能力やミスを責めるためではなく、以下を目的として行う。

- 要件を満たしているか確認する
- 不具合を早期に発見する
- コードを理解しやすくする
- 知識をチーム内で共有する
- 特定のメンバーだけが理解している状態を防ぐ
- セキュリティ上の問題を防ぐ

### 9.2 レビュー項目

レビュアーは、主に以下を確認する。

**仕様・機能**

- Issueの目的を満たしているか
- 完了条件を満たしているか
- 想定どおりに動作するか
- エラー時の処理が考慮されているか

**コード品質**

- 変数名や関数名が分かりやすいか
- 処理が必要以上に複雑になっていないか
- 同じ処理が重複していないか
- 1つの関数やコンポーネントに責務が集中していないか
- 不要なコードが残っていないか
- ほかのメンバーが理解できるか

**影響範囲**

- 既存機能を壊していないか
- APIやデータベースへの影響がないか
- 画面表示に意図しない変更がないか
- セキュリティ上の問題がないか

### 9.3 レビューコメントの分類

レビューコメントには、可能な限り分類を付ける。

```text
【必須】
マージ前に修正が必要な内容

【提案】
必須ではないが、よりよい実装方法の提案

【質問】
実装意図や仕様について確認したい内容

【軽微】
誤字、表記、命名などの小さな指摘

【参考】
今後役立つ情報や知識の共有
```

### 9.4 コメントの書き方

指摘する場合は、可能な限り理由を添える。

避ける例：

```text
ここを直してください。
この書き方はよくないです。
```

推奨例：

```text
【提案】
この処理は別の画面でも利用する可能性があるため、
共通関数に分けると再利用しやすくなると思います。
```

### 9.5 レビュー対応

Pull Request作成者は、レビューコメントに対して以下のいずれかを行う。

- 修正する
- 質問に回答する
- 修正しない理由を説明する
- 別Issueで対応することを提案する

コメントを確認せずに解決済みにしない。

### 9.6 レビュー期限

**推奨**

- レビュー依頼を受けた場合は、可能な限り1営業日以内に確認する
- すぐに確認できない場合は、その旨を共有する
- 緊急性の高いPull Requestは、チャットでも連絡する

---

## 10. コーディング規約

### 10.1 基本方針

**必須**

- プロジェクトで設定されたESLintやPrettierに従う
- Lintエラーを残したままマージしない
- 使用していない変数、関数、importを残さない
- デバッグ用のコードやログを残さない
- APIキーやパスワードをコードに直接書かない
- 意味を理解していないコードをそのまま使用しない

### 10.2 命名規則

原則として、以下の命名規則を使用する。

| 対象 | 規則 | 例 |
| --- | --- | --- |
| 変数 | camelCase | `userName` |
| 関数 | camelCase | `calculateTotalPrice` |
| Reactコンポーネント | PascalCase | `UserProfile` |
| 型・インターフェース | PascalCase | `UserData` |
| 定数 | UPPER_SNAKE_CASE | `MAX_LOGIN_ATTEMPTS` |
| ファイル | プロジェクト内で統一 | `user-profile.tsx`など |

良い例：

```ts
const userName = "Numata";

function calculateTotalPrice() {
  // 処理
}

const MAX_LOGIN_ATTEMPTS = 5;

function UserProfile() {
  return <div />;
}
```

避ける例：

```ts
const a = "Numata";
const data2 = [];
const tmp = {};
function aaa() {}
```

ループ処理など、役割が明確な場合は`i`などの短い変数名を使用してよい。

### 10.3 関数・コンポーネント

**推奨**

- 1つの関数には、可能な限り1つの責務を持たせる
- 長すぎる関数は分割を検討する
- 複雑な条件分岐は、関数への切り出しを検討する
- 同じ処理を複数箇所へコピーしない
- Reactコンポーネントが大きくなりすぎた場合は分割する
- UI表示とデータ取得処理の分離を検討する

### 10.4 TypeScript

TypeScriptを使用する場合は、以下を原則とする。

- `any`は原則として使用しない
- APIレスポンスやpropsには型を定義する
- 型が不明な値には、必要に応じて`unknown`を使用する
- 型エラーを無理に無効化しない
- `@ts-ignore`を使用する場合は理由をコメントする

### 10.5 コメント

コメントには、コードを読めば分かる内容ではなく、主に以下を書く。

- なぜその実装にしたのか
- 仕様上の制約
- 一見すると不要に見える処理の理由
- 外部サービスやライブラリ固有の注意点
- 将来的な改善事項

避ける例：

```ts
// ユーザー一覧を取得する
const users = await fetchUsers();
```

推奨例：

```ts
// APIへの負荷を抑えるため、初期表示では最大20件に制限する
const users = await fetchUsers({ limit: 20 });
```

### 10.6 TODOコメント

一時的に未対応事項を残す場合は、内容と関連Issueを記載する。

```ts
// TODO: #42 ページネーション対応時に取得件数を変更する
```

可能な限り、TODOコメントだけで管理せずIssueも作成する。

---

## 11. フォーマッター・Lint規約

### 11.1 使用ツール

プロジェクトでは、原則として以下を使用する。

- ESLint
- Prettier
- TypeScriptの型チェック

必要に応じて、以下の導入を検討する。

- Stylelint
- Husky
- lint-staged
- GitHub Actions

### 11.2 自動整形

コードの空白、改行、引用符、末尾のセミコロンなどは、原則としてPrettierの設定に従う。

個人の好みで整形ルールを変更しない。

### 11.3 設定変更

ESLintやPrettierの設定を変更する場合は、以下をPull Requestに記載する。

- 変更理由
- 影響範囲
- 既存コードへの影響
- 新しいルールへの対応方法

---

## 12. ディレクトリ・ファイル管理

### 12.1 基本ルール

- 同じ役割のファイルは同じディレクトリに配置する
- プロジェクト内の既存構成に従う
- 独自の判断で大規模なディレクトリ変更を行わない
- 使用していないファイルを残さない
- 自動生成ファイルを手動で編集しない
- 大規模な構成変更は、実施前にチームへ相談する

### 12.2 機能単位でまとめる（必須／適用は2026/08/06以降）

> **適用時期**：本項の全体適用はMVP公開（2026/08/06）以降とする。
> それまでは各担当者が選んだ進め方を優先し、未適用の機能があっても規約違反として扱わない。
> 適用範囲の調整は担当者間で行う（[decisions/0003](../decisions/0003-css-modules-and-feature-directories.md)）。

**役割ごとの層で分けたうえで、各層の中を機能名のディレクトリでさらに分ける。**

```text
app/
├── components/features/<機能>/   # 画面固有のコンポーネント
├── services/<機能>/              # データ取得・ドメインロジック
├── data/<機能>/                  # 静的データ
├── types/<機能>/                 # 型定義
└── lib/<機能>/                   # 純粋関数のユーティリティ
```

ディレクトリ名は機能名（`circle-info` / `chatbot` / `news` など）で、層をまたいで同じ名前を使う。

**ルール**

- 新しいファイルは、担当する機能のディレクトリに置く
- 複数機能で共有するものだけ、層の直下に置いてよい。
  どの機能が所有するかが明確なものは、共有されていてもその機能のディレクトリに置く
- 関連するファイルは同じ場所にまとめる。コンポーネントとそのCSS Modulesは同じディレクトリに、
  同じ名前で置く（`circle-card.tsx` ↔ `circle-card.module.css`）
- ルーティング（`app/routes/`）はファイル名がURLを決めるため、この分割の対象外とする

**理由**

複数人が並行して作業するとき、機能ごとに触るディレクトリが分かれるため、
コンフリクトとレビュー範囲の肥大化を防げる。
どのファイルがどの機能のものか、パスを見れば判断できる状態を保つ。

**データファイルを移動するときの注意**

`scripts/` の生成スクリプトは、出力先のパスと、生成するファイルに書き込む import 文を
文字列として持っている。移動時は両方を追随させる（片方だけだと次回実行時に壊れる）。
対応表は [architecture.md](./architecture.md) を参照。

### 12.3 ファイル名

ファイル名の規則は、技術構成に合わせて統一する。

例：

```text
components/
  user-profile.tsx

hooks/
  use-user.ts

services/
  user-service.ts

types/
  user.ts
```

Reactコンポーネントのファイル名をPascalCaseにする場合は、プロジェクト全体で統一する。

### 12.4 共通コンポーネント

ボタン、入力欄、モーダルなど、複数画面で使用するUIは共通コンポーネント化を検討する。

ただし、1回しか使用しないものを過度に共通化しない。

共通コンポーネントの見た目や利用方法は、別途定めるデザイン規約に従う。

---

## 13. ライブラリ管理規約

### 13.1 ライブラリ追加

新しいライブラリを追加する前に、以下を確認する。

- 標準機能で実現できないか
- すでに同じ目的のライブラリが入っていないか
- 現在も保守されているか
- ライセンス上の問題がないか
- プロジェクトの規模に対して過剰ではないか
- セキュリティ上の問題がないか

主要なライブラリを追加する場合は、事前またはPull Request上で共有する。

### 13.2 重複禁止

同じ目的のライブラリを複数導入しない。

例：

- 日付処理に`date-fns`と`dayjs`を併用しない
- HTTP通信に`fetch`と`axios`を無計画に混在させない
- 複数のUIライブラリを無計画に導入しない

### 13.3 lockファイル

以下のlockファイルはGitで管理する。

- `package-lock.json`
- `pnpm-lock.yaml`
- `yarn.lock`
- その他、使用するパッケージ管理ツールのlockファイル

チーム内では、原則として同じパッケージ管理ツールを使用する。

### 13.4 ライブラリ更新

大規模なバージョンアップは、通常の機能追加と分けて行う。

更新時は以下を確認する。

- 破壊的変更の有無
- ビルドが成功するか
- 既存機能が動作するか
- 設定ファイルの変更が必要か
- セキュリティ上の修正が含まれているか

---

## 14. 環境構築規約

### 14.1 バージョン統一

チーム内で、以下のバージョンを可能な限り統一する。

| 項目 | バージョン |
| --- | --- |
| Node.js |  |
| npm / pnpm / yarn |  |
| Python |  |
| データベース |  |
| その他 |  |

必要に応じて、以下を使用する。

- `.nvmrc`
- `.node-version`
- `.python-version`
- Docker
- Dev Container

### 14.2 README

READMEには、最低限以下を記載する。

- プロジェクト概要
- 使用技術
- 必要な環境
- リポジトリの取得方法
- ライブラリのインストール方法
- 環境変数の設定方法
- 開発サーバーの起動方法
- ビルド方法
- テスト方法
- デプロイ方法
- ディレクトリ構成

新しいメンバーがREADMEだけを読んで、開発環境を構築できる状態を目指す。

---

## 15. 環境変数・秘密情報

### 15.1 必須ルール

- `.env`をGitへ追加しない
- APIキーをコードに直接書かない
- パスワードをコードやIssueに記載しない
- アクセストークンをチャットへ貼り付けない
- 個人情報をテストデータとして使用しない
- 秘密情報をスクリーンショットに写さない

### 15.2 .env.example

必要な環境変数は、`.env.example`に記載する。

```env
DATABASE_URL=
NEXT_PUBLIC_API_BASE_URL=
AUTH_SECRET=
```

`.env.example`には、実際のAPIキーやパスワードを記載しない。

### 15.3 秘密情報を公開した場合

秘密情報を誤ってGitHubへpushした場合は、ファイルを削除するだけでは不十分である。

以下を実施する。

1. チームへ直ちに共有する
2. 該当するAPIキーやパスワードを無効化する
3. 新しいキーやパスワードを発行する
4. Git履歴からの削除が必要か確認する
5. 再発防止策を検討する

---

## 16. データベース・API規約

### 16.1 データベース変更

データベース構造を変更する場合は、以下を共有する。

- 変更するテーブル
- 追加・削除するカラム
- 既存データへの影響
- APIへの影響
- マイグレーション方法
- 本番環境への適用手順

本番環境のデータベースを、個人の判断で直接変更しない。

### 16.2 マイグレーション

- マイグレーションファイルはGitで管理する
- 既存のマイグレーションを無断で書き換えない
- 本番適用前に開発環境で確認する
- データ消失の可能性がある変更は事前に共有する

### 16.3 API変更

APIのURL、リクエスト、レスポンス形式を変更する場合は、利用する側の担当者へ共有する。

以下を明確にする。

- HTTPメソッド
- エンドポイント
- リクエスト形式
- レスポンス形式
- エラー形式
- 認証の有無
- 変更前後の差分

フロントエンドとバックエンドで認識が分かれないように、可能な限りAPI仕様を文書化する。

---

## 17. テスト・動作確認規約

### 17.1 Pull Request作成前の確認

Pull Requestを作成する前に、以下を確認する。

**必須**

- [ ] アプリが起動する
- [ ] 変更した機能が正常に動作する
- [ ] 既存機能が壊れていない
- [ ] Lintエラーがない
- [ ] 型エラーがない
- [ ] ビルドが成功する
- [ ] ブラウザコンソールに不要なエラーがない
- [ ] デバッグ用ログを削除した
- [ ] 不要なファイルを含めていない
- [ ] 秘密情報を含めていない

**画面変更がある場合**

- [ ] PC表示を確認した
- [ ] スマートフォン表示を確認した
- [ ] 極端に長い文章でも崩れない
- [ ] 画像が表示されない場合も考慮されている
- [ ] ボタンやリンクが操作できる
- [ ] デザイン規約に反していない

### 17.2 実行コマンド

使用する技術に応じて、以下のコマンドを実行する。

```bash
npm run lint
npm run type-check
npm run test
npm run build
```

プロジェクトに存在しないコマンドは省略してよい。

### 17.3 テストコード

テストを導入する場合は、特に以下を優先する。

- 認証処理
- 金額や数値の計算
- 入力値の検証
- データ変換処理
- 重要なAPI
- 不具合が発生しやすい処理
- 過去に不具合が発生した処理

すべてを自動テストするのではなく、重要度に応じて導入する。

---

## 18. デプロイ規約

### 18.1 デプロイ担当

本番環境へのデプロイ担当者をあらかじめ決める。

| 環境 | 担当者 |
| --- | --- |
| 開発環境 |  |
| ステージング環境 |  |
| 本番環境 |  |

### 18.2 本番デプロイ前の確認

- [ ] 対象のPull Requestがレビュー済みである
- [ ] `main`ブランチのビルドが成功している
- [ ] 必要な環境変数が設定されている
- [ ] データベース変更の影響を確認した
- [ ] リリース内容をチームへ共有した
- [ ] 問題発生時の戻し方を確認した
- [ ] 必要に応じてバックアップを取得した

### 18.3 本番環境で問題が発生した場合

1. 影響範囲を確認する
2. チームへ共有する
3. 必要に応じて以前のバージョンへ戻す
4. 原因を調査する
5. 修正Issueを作成する
6. 再発防止策を検討する

原因調査よりも先に、利用者への影響を止めることを優先する。

---

## 19. ドキュメント規約

### 19.1 更新が必要な場合

以下を変更した場合は、関連するドキュメントも更新する。

- 環境構築方法
- 使用技術
- 環境変数
- API仕様
- データベース構造
- 画面仕様
- ディレクトリ構成
- デプロイ方法
- 開発手順
- 重要な技術方針

### 19.2 重要な意思決定

以下のような重要な決定は、文章として残す。

- ライブラリを選んだ理由
- 技術構成を変更した理由
- 仕様を変更した理由
- 実装しないと決めた理由
- 複数案から選択した理由

記録は`docs/decisions/`にADR（連番＋内容が分かるファイル名）として残す。保存場所・命名規則はdocs/README.mdを参照。

記録には、最低限以下を含める。

```md
# 000X. 決定内容が分かるタイトル

- 日付:
- ステータス: 採用 / 却下 / 廃止

## 背景

## 決定

## 理由

## 影響
```

---

## 20. AI開発支援ツールの利用規約

本規約では、ChatGPT、Claude Code、GitHub Copilot、CodexなどをAI開発支援ツールとする。

### 20.1 利用可能な用途

AIツールは、以下の用途で利用してよい。

- コードの作成補助
- 不具合原因の調査
- リファクタリング案の作成
- テストコードの作成
- ドキュメントの作成
- コードレビューの補助
- 技術調査
- エラーメッセージの解釈

### 20.2 必須ルール

- AIへ秘密情報を入力しない
- 個人情報を入力しない
- APIキーやパスワードを入力しない
- AIが生成したコードは、利用者本人が内容を確認する
- 動作を理解できないコードをそのままマージしない
- AI生成コードも通常のコードレビュー対象とする
- 存在しないライブラリやAPIを使用していないか確認する
- ライセンス上問題のあるコードをコピーしない
- AIの回答を、公式ドキュメントより優先しない

### 20.3 AI生成コードの責任

AIが生成したコードであっても、コミットしたメンバーが内容に責任を持つ。

「AIが生成したため分からない」という状態でPull Requestを提出しない。

### 20.4 AIによる大規模変更

AIツールに大規模なコード変更を依頼する場合は、以下を守る。

- 作業前にGitの状態を保存する
- 変更対象を明確に限定する
- 変更内容をファイルごとに確認する
- 不要な変更が含まれていないか確認する
- Lint、ビルド、テストを実行する
- 一度に巨大なPull Requestを作らない

---

## 21. 不具合対応規約

### 21.1 不具合報告

不具合を発見した場合は、以下をIssueに記載する。

```md
## 発生した問題

## 発生環境

- OS：
- ブラウザ：
- 端末：
- 対象環境：開発 / ステージング / 本番

## 再現手順

1.
2.
3.

## 期待する動作

## 実際の動作

## エラーメッセージ

## スクリーンショット

## 担当・優先度

- 担当：
- 優先度：

## 補足
```

### 21.2 優先度

不具合の優先度は、以下を目安とする。

| 優先度 | 状態 |
| --- | --- |
| 緊急 | アプリ全体が利用できない、情報漏えいの可能性がある |
| 高 | 主要機能が利用できない |
| 中 | 一部機能に問題があるが回避方法がある |
| 低 | 軽微な表示崩れ、誤字、改善要望 |

---

## 22. 禁止事項

以下の行為を禁止する。

- `main`・`develop`ブランチへの直接push
- 秘密情報のコミット
- 他人のコードを無断で大幅に削除すること
- レビューを通さず本番反映すること
- 仕様を確認せず独断で変更すること
- 理解していないAI生成コードをマージすること
- 個人情報をテストデータとして使用すること
- チームへ共有せず主要ライブラリを変更すること
- 動作確認をせずPull Requestをマージすること
- 本番データベースを個人の判断で直接操作すること
- IssueやPull Request上でパスワードを共有すること

---

## 23. 開発の基本フロー

各メンバーは、原則として以下の手順で作業する。

1. 仕様書やデザインを確認する
2. Issueを作成または担当する（担当の個人が決まったらAssigneesに設定し、GitHub Projectsのステータスを`In Progress`にする）
3. Issueの目的と完了条件を確認する
4. 最新の`develop`ブランチを取得する
5. Issue用の作業ブランチを作成する
6. 実装する
7. 自分で動作確認する
8. Lint、型チェック、ビルド、テストを実行する
9. コミットする
10. リモートリポジトリへpushする
11. Pull Requestを作成する（本文に`close #Issue番号`を記載し、GitHub Projectsのステータスを`In Review`にする）
12. レビューを依頼する
13. 指摘に対応する
14. 承認後にマージする（`close #`の記載により、マージと同時にIssueが自動クローズされる）
15. Issueを終了する（GitHub Projectsのステータスを`Done`にする。自動化していない場合は手動で更新する）
16. Issueが終了した旨をSlackで連絡し、タスク管理スプレッドシートの完了プルダウンを選択する
17. 必要に応じてドキュメントを更新する

---

## 24. Definition of Done

Issueが完了したと判断する基準を、Definition of Doneとして定める。

以下をすべて満たした場合に、原則として完了とする。

- [ ] Issueの完了条件を満たしている
- [ ] 実装内容が仕様書と一致している
- [ ] デザイン規約に従っている
- [ ] ローカル環境で正常に動作する
- [ ] 既存機能を壊していない
- [ ] Lintエラーがない
- [ ] 型エラーがない
- [ ] ビルドが成功する
- [ ] 必要なテストが完了している
- [ ] Pull Requestがレビューされている
- [ ] レビュー指摘への対応が完了している
- [ ] 必要なドキュメントを更新している
- [ ] 秘密情報や不要なコードを含んでいない
- [ ] `develop`ブランチへマージされている
- [ ] GitHub ProjectsのステータスがDoneになっている

---

## 25. 例外対応

緊急時など、本規約どおりに進めることが難しい場合は、以下を実施する。

1. 例外対応が必要な理由を共有する
2. PMまたはSWEの了承を得る
3. 対応内容を記録する
4. 対応後に通常のレビューや確認を行う
5. 同様の問題が発生しないように見直す

緊急対応であっても、秘密情報や個人情報の管理規則は省略しない。

---

制定日：
最終更新日：
規約バージョン：Version 0.1
